name: goshawk_etl
channels:
  - conda-forge
  - terradue
  - defaults
dependencies:
  # Python 3.12 (compatible con esa_snappy y SNAP 13, mejor rendimiento)
  # esa_snappy soporta Python 3.9-3.12
  - python=3.12

  # SNAP Python interface (se instalará via pip después de SNAP)
  # esa_snappy será instalado por el script install_snap.sh

  # Geospatial libraries
  - rasterio
  - geopandas
  - shapely
  - fiona
  - pyproj

  # Scientific computing
  - numpy
  - scipy
  - scikit-learn
  - pandas

  # Visualization
  - matplotlib
  - seaborn

  # Image processing
  - scikit-image
  - opencv

  # Data download
  - requests

  # Utilities
  - tqdm
  - pyyaml
  - click

  # Development tools
  - jupyter
  - ipython

  # Pip packages (if not available in conda)
  - pip
  - pip:
    # Packages only available via pip
    - python-dotenv
    - asf_search  # Alaska Satellite Facility
    - xmltodict  # XML parsing for orbit files validation
    # Database integration for product traceability
    - sqlalchemy>=2.0.0
    - geoalchemy2>=0.14.0
    - psycopg2-binary>=2.9.9
    - rich>=13.7.0
    - tabulate>=0.9.0
    - -e ../satelit_metadata  # Satelit metadata database
